interface LeftPanelProps {
    docUrl: string;
}

const LeftPanel = (props: LeftPanelProps) => {

    const { docUrl } = props;

    return (
        <div>left panel</div>
    );
};

export default LeftPanel;